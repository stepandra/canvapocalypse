import { Icon as IconI } from '../../../types';
interface Props {
    icon: IconI;
    onClick?: () => void;
    onMouseDown?: () => void;
}
export declare const Icon: ({ icon, onClick, onMouseDown }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
