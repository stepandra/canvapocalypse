import { ViewItem } from '../../../../types';
interface Props {
    node: ViewItem;
    order: number;
    isDimmed?: boolean;
}
export declare const Node: ({ node, order, isDimmed }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
